[user]
	name = Gerardo Munguia
	email = gerardomunguiahoyo@gmail.com
[core]
	excludesfile = ~/.gitignore
	editor = nvim
[push]
	default = simple
[alias]
	vimdiff = mergetool -t vimdiff
	co = checkout
	cp = cherry-pick
	fa = ! git fetch origin -p && git fetch upstream -p
	k = log --graph --oneline --all
	nuke = ! git branch | grep -v master | xargs git branch -D && git gc
	rum = ! git fetch upstream && git rebase upstream/master
	cane = ! git commit --amend --no-edit
	pff = ! git branch | grep \\* | sed 's/\\* //' | xargs git push --force-with-lease origin
[diff]
	tool = vimdiff
[merge]
	tool = vimdiff
	conflictstyle = diff3
[mergetool "vimdiff"]
	cmd = nvim -d $BASE $LOCAL $REMOTE $MERGED -c '$wincmd w' -c 'wincmd J'
[gui]
	gcwarning = false
